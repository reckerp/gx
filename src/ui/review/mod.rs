//! The `gx review` TUI: syntax highlighting (U3), the side-by-side diff widget
//! and event loop (U4), the file-tree sidebar (U5), and the comment overlay
//! (U6) are wired together here as the units land.

pub mod highlight;
